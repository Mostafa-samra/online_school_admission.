import 'package:flutter/material.dart';

import 'widget/page21_payment_successful_view_body.dart';

class Page21PaymentSuccessfulView extends StatelessWidget {
  const Page21PaymentSuccessfulView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Page21PaymentSuccessfulViewBody(),);
  }
}